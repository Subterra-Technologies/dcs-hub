*filter
:INPUT DROP [0:0]
:FORWARD DROP [0:0]
:OUTPUT ACCEPT [0:0]

-A INPUT -i lo -j ACCEPT
-A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
-A INPUT -m conntrack --ctstate INVALID -j DROP

# Management SSH: only from the ops network.
-A INPUT -s __MGMT_CIDR__ -p tcp --dport 22 -j ACCEPT

# Enrollment API (HTTPS). Pis hit this from the public internet.
-A INPUT -p tcp --dport 8443 -j ACCEPT

-A INPUT -p icmp -j ACCEPT

# Coordinator does not route traffic between schools and Zabbix VMs.
# Each Zabbix VM runs its own WG tunnel directly to its paired Pi.

COMMIT
